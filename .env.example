# RoomZero backend runtime environment example

ROOMZERO_ENV=local

# Docker runtime DB location (mapped by docker-compose named volume)
ROOMZERO_PLATFORM_DB_PATH=/app/data/platform/platform.sqlite

# Optional model configuration
OPENAI_MODEL=gpt-4o-mini

# Optional: set if using OpenAI mode
# OPENAI_API_KEY=
